#include<stdio.h>
#include<conio.h>
int main()
{
    int a[10],i,max;
    i=1;
    max==a[0];

    printf("enter ten no");
    scanf("%d",&a[10]);
    printf("largest no in the given array is:");
    for(i=1;i<11;i++)
   { 
      if
       {
          max<a[i];
          max==a[i];
      }
      else
      continue;
   }
   printf("max is %d",max)

}